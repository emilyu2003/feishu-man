name: feishu-man
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - langchain
      - langchain-openai
      - langgraph
      - pydantic
      - pydantic-settings
      - python-dotenv
      - pandas
      - nodejs # 用于安装 lark-cli
